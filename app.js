const hobbies = require ('./hobbies.js');
const names = require ('./names.js');
console.log(hobbies.creaHobby)

const persona = ()=>{
    hobbies.creaHobby(a, b, c);
    names.fullName (d, f);    
};

